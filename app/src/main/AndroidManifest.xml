<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alquilerpelicula">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlquilerPelicula">
        <activity android:name=".ViendoPelicula"></activity>
        <activity android:name=".VerTrailer" />
        <activity android:name=".paginaprincipal" />
        <activity android:name=".mostrarpelicula" />
        <activity android:name=".PeliculaComprada" />
        <activity android:name=".ComprarPelicula" />
        <activity android:name=".registro" />
        <activity android:name=".registroTarjeta" />
        <activity android:name=".login" />
        <activity android:name=".interfaceinicio"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>